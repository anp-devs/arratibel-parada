/*body.onload(function() {
    console.log("Hello works");
    
    var distancia = $('#bar').offset().top;

    $(window).on('scroll', function() {

        if ($(window).scrollTop() > distancia) {

            $('#bar').addClass('sticky-bar');

        } else if ($(window).scrollTop() <= 0 || $(window).scrollTop() < distancia) {

            $('#bar').removeClass('sticky-bar');

        }

    })

});*/

/*window.onload = function() {

    var distancia = document.getElementById('bar').offsetTop;
    var barra = document.getElementById('bar');
    window.onscroll = function() {
        if (window.scrollY > 105) {

            barra.classList.add('sticky-bar');

        } else if (window.scrollY <= 0 || window.scrollY() < 105) {
            barra.classList.remove('sticky-bar');
        }
    }

}*/